<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="46dp"
    android:gravity="left"
    android:paddingLeft="10dp"
    android:background="@color/bg_main_gray"
    android:id="@+id/item_category_main_rl"
    >

    <Button
        android:id="@+id/btnDeleteCategory"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="5dp"
        android:background="@drawable/ic_delete_category"
        android:layout_centerVertical="true"
        android:gravity="center"
        android:visibility="visible"
        android:textColor="@color/white"
         />
    <TextView
        android:id="@+id/tvCategoryName"
        android:layout_toRightOf="@id/btnDeleteCategory"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginLeft="13dp"
        android:padding="10dp"
        android:gravity="center"
        android:textColor="@color/bg_nav_bar"
        android:text="默认分类"
         />
<Button
    android:id="@+id/btnEditCategory"
    android:layout_width="25dp"
    android:layout_height="25dp"
    android:layout_marginRight="20dp"
    android:visibility="gone"
    android:layout_centerVertical="true"
    android:layout_alignParentRight="true"
    android:gravity="center"
    android:background="@drawable/ic_edit_category"
    />
    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:background="@color/gray"
        android:layout_marginLeft="12dp"
        android:layout_alignParentBottom="true"
        />

</RelativeLayout>
